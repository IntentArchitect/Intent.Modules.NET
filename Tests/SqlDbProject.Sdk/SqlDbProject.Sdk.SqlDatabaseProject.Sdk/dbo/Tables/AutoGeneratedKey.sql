CREATE TABLE [dbo].[AutoGeneratedKey]
(
    [Id] UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID(),
    [Name] NVARCHAR(MAX) NOT NULL,
    [Value] NVARCHAR(MAX) NOT NULL,
    CONSTRAINT [PK_AutoGeneratedKey] PRIMARY KEY CLUSTERED ([Id] ASC)
);